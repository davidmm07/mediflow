# Normalise to LF in the repository. A checkout on Windows would otherwise
# give CRLF endings to files that are read inside Linux containers, such as
# the Keycloak realm import, and the failures that causes are reported far
# from their cause.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gz  binary
