// Deliberately dependency-free: the smoke test is the one thing that must run
// against an unfamiliar machine without setup, so it uses nothing but the
// standard library.
module github.com/davidmm07/mediflow/tools/smoke

go 1.25.0
